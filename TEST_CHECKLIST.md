# 旅レートカメラ 実機確認チェックリスト（2026-09-11 現行版）

**確認日**:
**確認者**:
**ビルド**: Preview Build（EAS `preview` profile）
- **Build ID**：`fa58940a-03c5-4d70-8d4b-16bc588cc1a5`
- **対象commit**：`ba2153a2b9387a4e8fb617d7b5ef5f5870bb8e03`（`ba2153a`）
- **Build日時**：2026-09-12 09:03〜09:09（JST）／status FINISHED
- **install**：https://expo.dev/accounts/en540424/projects/travel-rate-camera-app/builds/fa58940a-03c5-4d70-8d4b-16bc588cc1a5
- **app version / buildNumber**：1.0.0 / 1（SDK 56.0.0・distribution internal）

**前提**: 第三者監査（Fable 5.1／Codex）統合修正後の実機確認。**旧Preview Buildは対象外**（OTA更新は使っていないため、新Buildでないと修正が実機に載らない）。

> **このBuild以外で確認しない。** 第三者監査の統合修正（S1全件・S2 A〜I）は`ba2153a`時点で入っており、
> 直前の2026-09-10 Preview Build（commit `4f6ef24b`）には**17commit分の修正が載っていない**。

> 旧版（2026-07-03の「公開前P0コードバッチ」等、Pro非公開・保存30件・Pro導線非表示を前提とした項目）は本版で全面差し替え。旧内容はgit履歴を参照。

## 確定仕様（このチェックリストの前提・2026-09-11 Human確定）

| 項目 | 無料版 | Pro |
|---|---|---|
| 保存件数 | **1つの旅行につき10件**（旅行ごと。端末全体の上限ではない。`FREE_LIMITS.saves = 10`、判定は現在の旅行の件数） | 無制限 |
| 旅行数 | **同時に管理できる旅行は1件**（非アーカイブの旅行数で判定。アーカイブすれば新規作成可。アーカイブ済み旅行の記録は保持・閲覧可。復元も同じ境界） | 複数旅行を同時に管理 |
| カテゴリー | 保存は可。絞り込み・分析は不可 | 絞り込み・分析可 |
| CSV書き出し | 不可（ボタンは表示、押すとPro画面へ） | 可（新しい方から最大500件） |
| 価格 | — | 月額 ¥500／年額 ¥4,000（自動更新。**アプリ内表示はRevenueCatの`priceString`。App Store Connectの設定と一致していること**） |
| 残り予算 | 予算 − 購入済み合計（候補は差し引かない） | 同じ |

## 使い方

- 上から順に実行する（環境準備 → 基本フロー → 上限・Pro → 課金 → 翻訳 → その他 → 提出前）
- 各項目：**操作／期待結果／NG例／実機／Sandbox**。「実機：必須」はSimulator不可、「Sandbox：必須」はSandbox Apple IDでのログインが必要
- 判定は `[x]` OK／`[ ]` 未確認／`[!]` NG（NG内容を末尾の「問題メモ」へ）
- 無料版の確認は**Pro未購入（または失効済み）状態**で行う。Pro項目は購入後に行う

---

## 確認順の優先度（P0先行・2026-09-12追加）

> [!note] これは**どれを先に確認するか**の索引であり、末尾の「問題発見時の優先順位」（＝
> 見つかった不具合の**深刻度**）とは別の軸。項目番号は**一切変更していない**
> （`#27〜#36` 等が他文書から参照されているため）。全54項目をやめる索引ではなく、
> **時間が足りないときに何から潰すか**を決めるためのもの。

### P0：App Store submissionへ直結（ここがNGなら提出しない）

| 分類 | 項目# |
|---|---|
| 起動・install | #1 上書きinstall／#2 新規install／#4 起動 |
| migration・データ保持 | #3 migration／既存データ保持 |
| OCR | #6 OCR |
| 保存 | #8 商品写真／#9 保存（候補／購入済み） |
| 無料gate | #11 1旅行10件／#13 10/10到達／#15 旅行作成制限／#17 restore gate |
| 価格表示 | #27 RevenueCat価格／#28 月額¥500／#29 年額¥4,000 |
| 購入 | #30 purchase／#36 purchase完了後 entitlement反映 |
| 復元・失効 | #32 restore／#33 Pro失効 |
| 法務リンク | #37 Privacy／Termsリンク |
| 音声 | #39 STT／#41 TTS |
| 写真・削除 | #48 写真削除／#49 データ全削除 |
| 提出物 | #52 アプリアイコン |

**P0がすべて `[x]` になるまで production Build へ進まない。**
特に #27〜#29（価格）はApp Store Connect／RevenueCatの設定と一致している必要があり、
提出文言（Vault提出パッケージ §A-4 の説明文に記載した ¥500／¥4,000）とも突き合わせる。

### P1：release品質（提出は止めないが、出す前に確認したい）

上記P0以外の全項目（#5・#7・#10・#12・#14・#16・#18〜#26・#31・#34・#35・#38・#40・#42〜#47・#50・#51・#53）。

特に #34 offline起動→online復帰 と #35 Offering再取得 は、
価格が取得できない状況での見え方に関わるため、時間があれば優先的に確認する。

---

## A. 環境準備（install・起動）

### 1. 上書きinstall（既存Preview Build → 新Preview Build）
- [ ] 操作：旧Preview Build入りの端末に、新Buildを上書きinstallして起動
- 期待：起動し、既存の旅行・記録・写真・設定がそのまま残っている
- NG例：起動クラッシュ／旅行が消える／写真だけ消える／レートが初期値に戻る
- 実機：必須／Sandbox：不要

### 2. 新規install（アプリを削除してから）
- [ ] 操作：アプリを削除 → 新Buildをinstall → 起動
- 期待：初回状態（旅行なし）で起動。旅行作成 → 保存まで通る
- NG例：初回起動でクラッシュ／DB初期化エラー／保存できない
- 実機：必須／Sandbox：不要
- 補足：v3 migrationのtransaction化（`06b26bf`）は**新規installの初回起動経路でしか実機実行されない**。新規install確認は必須

### 3. migration／既存データ保持
- [ ] 操作：項目1の上書き後、履歴・カレンダー・分析・旅行一覧・データ管理を一通り開く
- 期待：件数・金額・写真・カテゴリー・購入済み/候補の状態が上書き前と同じ
- NG例：件数が減る／写真が「読み込めません」／カテゴリーが未分類に戻る
- 実機：必須／Sandbox：不要

### 4. 起動（通常起動・バックグラウンド復帰）
- [ ] 操作：通常起動、ホームへ戻して復帰、タスクキルして再起動
- 期待：毎回同じ旅行がアクティブで表示される。復帰時に白画面・固まりがない
- NG例：復帰時に旅行未選択になる／カメラプレビューが真っ黒のまま
- 実機：必須／Sandbox：不要

---

## B. 基本フロー（撮影 → 保存）

### 5. カメラ撮影（値札）
- [ ] 操作：カメラタブで値札を撮影
- 期待：プレビューが出て、撮影後に読み取り結果（金額候補）が表示される
- NG例：撮影ボタンが反応しない／撮影後に画面が進まない
- 実機：必須／Sandbox：不要

### 6. OCR
- [ ] 操作：主要通貨（USD／EUR／JPY／KRW／THB／TWD／GBP）の値札を撮影し、金額候補を確認
- 期待：価格候補が出て、選ぶと円換算される。メモ候補（商品名・容量等）が出る
- NG例：常に候補ゼロ／桁が明らかに違う（4,500 → 4.5等）
- 実機：必須／Sandbox：不要
- 補足：撮影条件による揺れは既知仕様（`mvp-tasks.md`「KRW/JPY価格OCR改善：条件付き合格」）。**同じ値札で毎回失敗する**場合のみNG

### 7. 手入力
- [ ] 操作：OCRを使わず金額を手入力 → 円換算 → 保存。換算タブ（converter）でも同様
- 期待：換算額が表示され保存できる。保存後に入力欄がクリアされる
- NG例：小数点入力不可／保存ボタンが押せない／保存後も入力が残る
- 実機：必須／Sandbox：不要

### 8. 商品写真
- [ ] 操作：商品写真を撮る（カメラ権限を一度拒否してから許可する経路も）
- 期待：撮影前に権限確認があり、拒否時は設定への導線が出る（`ad8b67d`）。撮影後にサムネイルが出る
- NG例：権限拒否で無反応／写真が保存後に表示されない
- 実機：必須／Sandbox：不要

### 9. 保存（候補／購入済み）
- [ ] 操作：候補で保存 → 履歴で確認。購入済みで保存 → 履歴で確認
- 期待：保存トーストが出て、履歴に反映。保存時のレートが記録される（後でレートを変えても円額が変わらない）
- NG例：保存後に履歴に出ない／レート変更で過去の円額が変わる
- 実機：必須／Sandbox：不要

### 10. 保存連打
- [ ] 操作：保存ボタンを素早く2〜3回連打（カメラ・換算タブの両方）。旅行作成の「作成」も連打
- 期待：**1件だけ**保存される。ボタンがloading表示になり二重保存されない（`d7fb28d`）
- NG例：同じ記録が2件できる／旅行が2件できる
- 実機：必須／Sandbox：不要

---

## C. 無料版の上限（保存10件＝旅行ごと／旅行は同時に1件）

### 11. 1旅行10件制限
- [ ] 操作：無料版で1つの旅行に10件保存し、11件目を保存しようとする
- 期待：11件目で「この旅行の保存上限に達しました」シート（`SaveLimitSheet`）が出て保存されない。「Proにアップグレード」でPro画面へ、「あとで」で閉じる
- NG例：11件目が保存できる／10件目で止まる／シートが出ずに無反応
- 実機：必須／Sandbox：不要

### 12. 7/10以降の残数表示
- [ ] 操作：7件目を保存した状態でカメラタブを見る（撮影前のホーム画面と、撮影・手入力中の入力カード）
- 期待：7件目から「この旅行の保存件数 7 / 10 件」バナー（入力カード内）と、下部サマリーの「保存 残り3件」が出る。6件以下では出ない
- NG例：6件で出る／10件でも出ない／件数が別の旅行の分を含む
- 実機：必須／Sandbox：不要

### 13. 10/10到達
- [ ] 操作：10件目を保存
- 期待：バナーが「10 / 10 件」で警告色。下部サマリーが「残り0件」。以後の保存はシート表示（項目11）
- NG例：10/10で表示が9/10のまま／保存が通る
- 実機：必須／Sandbox：不要

### 14. 既存データの閲覧／編集／削除（上限到達後）
- [ ] 操作：10/10の状態で、履歴の閲覧・編集（金額・メモ・状態・カテゴリー・写真）・1件削除 → 削除後に1件保存
- 期待：上限到達後も閲覧・編集・削除は自由。1件削除すると9/10になり再び保存できる
- NG例：編集が保存できない／削除しても件数が減らない
- 実機：必須／Sandbox：不要

### 15. 旅行作成制限（同時に1件）
- [ ] 操作：無料版で旅行が1件ある状態で「新しい旅行を作成」
- 期待：「無料版で同時に使える旅行は1件までです」シート（`TripLimitSheet`）が出て作成されない
- NG例：2件目が作れる／シートが出ない
- 実機：必須／Sandbox：不要

### 16. archive
- [ ] 操作：旅行編集からアーカイブ → 新しい旅行を作成 → 新旅行で保存 → 旅行一覧でアーカイブ済み旅行の記録を開く
- 期待：アーカイブ後は新規作成できる。**新旅行は0/10から保存できる**（保存上限は旅行ごと）。アーカイブ済み旅行の記録は残り、閲覧できる
- NG例：アーカイブ後も作成できない／新旅行の件数が旧旅行分を引き継ぐ／アーカイブで記録が消える
- 実機：必須／Sandbox：不要

### 17. restore gate（アーカイブ復元）
- [ ] 操作：無料版で非アーカイブ旅行が1件ある状態で、アーカイブ済み旅行を「復元」
- 期待：復元されず`TripLimitSheet`が出る（`fa79d3f`）。非アーカイブ旅行が0件なら復元できる
- NG例：復元で旅行が2件同時に使える状態になる／復元失敗Alertだけ出てPro導線がない
- 実機：必須／Sandbox：不要

### 18. Proなら複数旅行
- [ ] 操作：Pro購入後（項目30以降）に旅行を2件以上作成し、切り替えて保存
- 期待：作成・復元とも無制限。各旅行の件数・残り予算が独立
- NG例：Proでも`TripLimitSheet`が出る
- 実機：必須／Sandbox：必須

### 19. 残予算（budget − purchased、候補は引かない）
- [ ] 操作：予算を設定した旅行で、候補1件・購入済み1件を保存し、カメラタブ・履歴・設定・旅行一覧・旅行切替シート・分析の残り予算を見比べる
- 期待：**全画面で「予算 − 購入済み合計」**。候補は差し引かれない。501件超でも集計が全件（`a0fade9`）
- NG例：画面によって値が違う／候補を引いた値になる
- 実機：必須／Sandbox：不要

---

## D. カテゴリー・CSV・大量データ

### 20. Category保存
- [ ] 操作：無料版でカテゴリーを選んで保存
- 期待：無料版でも保存でき、履歴・詳細に表示される
- NG例：無料版でカテゴリー選択が出ない
- 実機：必須／Sandbox：不要

### 21. Category reset（保存後・破棄）
- [ ] 操作：カテゴリーを選んで保存 → 次の商品の入力画面。履歴編集でカテゴリーを変えて「破棄」
- 期待：保存後は次の商品のカテゴリーが未選択に戻る。編集破棄で元のカテゴリーへ戻る（`c639c41`）
- NG例：前回のカテゴリーが次の商品に残る／破棄しても変更後のまま
- 実機：必須／Sandbox：不要

### 22. Category edit
- [ ] 操作：履歴編集でカテゴリーを変更して保存 → 詳細・一覧・分析で反映を確認
- 期待：即時反映。写真・金額・メモ・状態は変わらない
- NG例：保存したのに一覧が旧カテゴリー
- 実機：必須／Sandbox：不要

### 23. Category filter（Pro）
- [ ] 操作：無料版では履歴に絞り込みUIが出ないことを確認。Pro購入後に履歴でカテゴリー絞り込み
- 期待：無料版：UIなし。Pro：選んだカテゴリーだけ表示、解除で全件。失効後は絞り込みが自動解除
- NG例：無料版で絞り込める／Proで絞り込んでも全件のまま
- 実機：必須／Sandbox：必須（Pro側）

### 24. Category analysis（Pro）
- [ ] 操作：分析タブのカテゴリー別（今日／今月／今年）
- 期待：無料版：Pro導線カードのみ。Pro：カテゴリー別の合計金額・件数・構成比が期間と連動
- NG例：期間を変えても値が変わらない／合計が上の集計と食い違う
- 実機：必須／Sandbox：必須（Pro側）

### 25. CSV export（Pro）
- [ ] 操作：無料版でデータ管理の「CSV書き出し」を押す。Pro購入後に書き出して共有シートからファイルアプリへ保存し、開く
- 期待：無料版：Pro画面へ。Pro：BOM付きUTF-8で日本語が文字化けしない。列（日付・金額・円換算・状態・カテゴリー・メモ等）が揃う
- NG例：共有シートが出ない／Excelで文字化け
- 実機：必須／Sandbox：必須（Pro側）

### 26. 500件超表示
- [ ] 操作：（Proで）1旅行に501件以上保存できる場合、履歴・データ管理・CSVを確認。用意できない場合は「未実施」と記録
- 期待：履歴一覧は新しい500件。データ管理の件数（保存件数・候補・購入済み）は**全件**（`8b145c5`）。CSVは「書き出せるのは新しい方から500件」Alertが出る
- NG例：データ管理の件数が500で止まる／CSVが黙って500件で切れる
- 実機：必須／Sandbox：必須（Pro）
- 補足：分析・カレンダーの日付別集計は全旅行2000件窓（2001件超は既知仕様。§46）

---

## E. 課金（RevenueCat／Sandbox）

> Sandbox Apple IDで実行する。**production Build・実課金では行わない**。`[PurchaseDiag]`のログはXcode／Console.appで確認。

### 27. RevenueCat価格
- [ ] 操作：設定 →「現在のプラン」→ Pro画面 → プラン選択
- 期待：「価格を読み込み中…」のあと月額・年額の価格が表示される（Offering `default`）
- NG例：価格が出ない／片方だけ出る／読み込み中のまま
- 実機：必須／Sandbox：必須

### 28. 月額 ¥500
- [ ] 操作：月額Packageの表示価格を確認
- 期待：**¥500**（App Store Connectの設定値）。異なる場合はASC側の設定を確認（アプリはハードコードしていない）
- NG例：¥400／¥480など旧価格が表示される
- 実機：必須／Sandbox：必須

### 29. 年額 ¥4,000
- [ ] 操作：年額Packageの表示価格を確認
- 期待：**¥4,000**
- NG例：¥3,000／¥3,800など旧価格
- 実機：必須／Sandbox：必須

### 30. purchase
- [ ] 操作：月額（または年額）を購入 → Sandboxのシートで承認
- 期待：購入完了画面 → Pro解放（保存無制限・旅行複数・カテゴリー絞り込み/分析・CSV）。設定の「現在のプラン」がPRO表示
- NG例：完了画面が出るのに無料版上限が残る／PROバッジが出ない
- 実機：必須／Sandbox：必須

### 31. cancel（購入キャンセル）
- [ ] 操作：購入シートでキャンセル
- 期待：エラーAlertを出さずに購入画面へ戻る。Proにならない
- NG例：キャンセルで「購入に失敗」Alert／キャンセルなのにProになる
- 実機：必須／Sandbox：必須

### 32. restore
- [ ] 操作：Pro購入済みのSandboxアカウントで、アプリを削除 → 再install → 「購入を復元」
- 期待：「購入を復元しました」→ Pro解放。未購入アカウントでは「復元できる購入が見つかりませんでした」
- NG例：復元しても無料版のまま／未購入で成功表示
- 実機：必須／Sandbox：必須

### 33. Pro失効
- [ ] 操作：Sandboxで自動更新を止める／期限切れを待つ（Sandbox月額は約5分）→ アプリをフォアグラウンド復帰
- 期待：無料版へ戻る。**既存の記録・複数旅行は削除されない**。11件目の保存・新規旅行作成・復元だけが無料版gateにかかる。カテゴリー絞り込みが解除される
- NG例：失効でデータが消える／失効後もProのまま／複数旅行の切替ができなくなる（現行仕様は切替可）
- 実機：必須／Sandbox：必須

### 34. offline起動 → online復帰
- [ ] 操作：機内モードで起動 → Pro画面を開く → 機内モード解除 → フォアグラウンド復帰
- 期待：offline時は無料機能が使える。価格は「読み込み中…」または再取得ボタン。online復帰で価格が取れる（`a213b9a`）
- NG例：offline起動でクラッシュ／online復帰後も価格が出ない
- 実機：必須／Sandbox：必須

### 35. Offering再取得
- [ ] 操作：項目34の状態で購入画面の「価格情報を再取得」を押す
- 期待：価格が表示される。連打しても要求が1本にまとまる（無限retryなし）
- NG例：押しても変化なし／エラーで固まる
- 実機：必須／Sandbox：必須

### 36. purchase完了後 entitlement反映
- [ ] 操作：購入直後に設定の「現在のプラン」、カメラタブ（バナー消滅）、履歴（絞り込みUI出現）、データ管理（CSVのProバッジ消滅）を確認
- 期待：再起動なしで全画面がPro表示。entitlement未反映時は「購入を復元」誘導Alert（`entitlement_missing`経路）
- NG例：再起動しないとProにならない
- 実機：必須／Sandbox：必須

### 37. Privacy／Termsリンク
- [ ] 操作：購入確認画面と設定＞アプリ情報から、利用規約・プライバシーポリシーを開く
- 期待：公開ページ（`travel-rate-camera-lp.vercel.app/privacy`・`/terms`）が開き、**Pro・自動更新・解約方法・RevenueCat・音声認識**の記述が現行版（LP `01e0588`以降）
- NG例：「準備中」Alert／404／「課金なし」「外部送信なし」の旧文言
- 実機：必須／Sandbox：不要

---

## F. 翻訳ページ（Translation／STT／TTS）

### 38. Translation
- [ ] 操作：翻訳タブで自由入力 → 翻訳。swap、言語ピッカーで片側だけ変更、コピー、クリア
- 期待：翻訳結果が出る。swap・片側変更で反対側が旅行設定へ巻き戻らない。1000文字上限
- NG例：swapで言語が戻る／翻訳が返らない
- 実機：必須／Sandbox：不要

### 39. STT（音声入力）
- [ ] 操作：マイクをtap → 権限許可 → 話す → 停止
- 期待：認識中の文字が入力欄に反映され、停止後も残る。翻訳へ繋がる
- NG例：停止と同時に文字が消える／権限拒否で設定導線がない
- 実機：必須／Sandbox：不要

### 40. STT権限待ち中離脱
- [ ] 操作：初回のマイク権限ダイアログが出ている間に別タブへ移動 → 許可 → 翻訳タブへ戻る
- 期待：**マイクが勝手に起動しない**（`f282c03`）。戻った後に手動でtapすれば動く
- NG例：戻ったら録音中になっている／マイクindicatorが残る
- 実機：必須／Sandbox：不要

### 41. TTS（読み上げ）
- [ ] 操作：訳文を読み上げ
- 期待：target言語で読み上げ。対応外言語では「読み上げには対応していません」
- NG例：無音／別言語の声
- 実機：必須／Sandbox：不要

### 42. TTS stop／画面離脱
- [ ] 操作：読み上げ中に停止tap。読み上げ中にカメラタブへ移動。停止直後にすぐ再生
- 期待：即停止。離脱で止まり、audio session・マイクindicatorが残らない。停止直後の再生が遅れて二重に鳴らない（`f282c03`）
- NG例：離脱後も鳴り続ける／停止後に遅れて再生が始まる
- 実機：必須／Sandbox：不要

### 43. voice選択
- [ ] 操作：「読み上げ設定」で声を変更・試聴 → 「自動」へ戻す → アプリ再起動後も設定が残るか
- 期待：選択が即反映・永続化。別言語のvoiceへ落ちない
- NG例：再起動で選択が消える／試聴と本番で声が違う
- 実機：必須／Sandbox：不要

---

## G. その他画面

### 44. カレンダー
- [ ] 操作：月移動、日付tap、月合計・選択日合計・選択日の記録
- 期待：見出しで月／日が区別できる。国旗がはみ出さない。記録が日付に正しく紐づく
- NG例：日付tapで別日の記録が出る
- 実機：必須／Sandbox：不要

### 45. 分析
- [ ] 操作：今日／月／年の切替、棒tap、旅行別購入済み
- 期待：期間に応じた集計。棒tapで金額カード。残り予算は購入済みのみ控除
- NG例：期間切替で合計が変わらない
- 実機：必須／Sandbox：不要

### 46. 500／2000件に関する既知仕様
- [ ] 操作：確認のみ（大量データを用意できる場合は項目26と同時に）
- 期待：**既知仕様として受容**：履歴一覧は表示500件、CSVは500件（Alertあり）、分析・カレンダーの日付別は全旅行2000件窓。金額集計（残り予算・購入済み合計・データ管理件数）は全件
- NG例：金額集計が表示窓で切れる
- 実機：任意／Sandbox：不要

### 47. 写真編集（item-edit）
- [ ] 操作：履歴編集で写真を削除・再撮影・ライブラリ選択 → それぞれ「破棄」→ 元画像が残るか。保存 → 旧ファイルが消えるか
- 期待：保存するまでdraft、破棄で完全復帰。写真→金額→メモ→状態→カテゴリーの順に確定し、途中失敗でも新写真が消えない（`c639c41`）
- NG例：破棄しても元画像に戻らない／ライブラリが開かない
- 実機：必須／Sandbox：不要

### 48. 写真削除（記録削除時）
- [ ] 操作：写真付き記録を削除
- 期待：DB削除成功後に写真ファイルが消える。削除失敗時は写真が残り記録も残る
- NG例：記録が残るのに写真だけ消える
- 実機：必須／Sandbox：不要

### 49. データ全削除
- [ ] 操作：データ管理 →「危険な操作」→ 全削除（確認Alertあり）
- 期待：確認Alert「この旅行の履歴を削除しますか？」→ この旅行の記録・写真が全件消える（表示500件に依存しない）。旅行・設定・Pro状態・他の旅行の記録は残る
- NG例：501件目以降の写真が残る／旅行まで消える
- 実機：必須／Sandbox：不要

### 50. 権限拒否時（カメラ・フォト・マイク・音声認識）
- [ ] 操作：各権限を拒否した状態で該当機能を使う
- 期待：クラッシュせず、設定への導線または案内文が出る。権限文言（Info.plist）が用途を具体的に説明している
- NG例：無反応／クラッシュ／英語の既定文言
- 実機：必須／Sandbox：不要

### 51. シャッター音
- [ ] 操作：値札撮影・商品写真撮影
- 期待：iOS既定のシャッター音（アプリ側で制御していない）。マナーモードでの挙動もOS既定
- NG例：撮影自体が失敗する
- 実機：必須／Sandbox：不要

### 52. アプリアイコン
- [ ] 操作：ホーム画面・設定・Spotlightでアイコンを確認
- 期待：黒背景や角が欠けない。alphaによる黒枠なし（元画像はRGBA。Expo pluginの白背景除去を最終生成物で確認）
- NG例：黒い四隅／ぼやけ
- 実機：必須／Sandbox：不要

### 53. splash
- [ ] 操作：コールドスタート
- 期待：splashが表示され、白画面・ちらつきなしでカメラタブへ
- NG例：splashが縦横比崩れ／長時間固まる
- 実機：必須／Sandbox：不要

---

## H. 提出前確認（production Build前・Human-only）

### 54. production提出前確認
- [ ] 本チェックリストのA〜Gに`[!]`が残っていない（または対応方針が決まっている）
- [ ] `eas.json` productionの`autoIncrement: true`（【2026-09-12】Human確定・適用済み。詳細`RELEASE_PREP.md`§1）が意図通り効いていることをproduction Build実物で確認
- [ ] App Store Connect：Pro商品（月額¥500／年額¥4,000）とEntitlement `pro`／Offering `default`の紐付け、価格、契約同意、Banking/Tax
- [ ] App Privacy申告がVault提出パッケージ **§B** の事実と一致（RevenueCat購入履歴・Apple音声認識）。**§B-3の公式確認5件を先に済ませる**
- [ ] ASCの各入力欄がVault提出パッケージ **§D-2** の値と一致（説明文・キーワード・カテゴリ・Copyright）
- [ ] Age Rating回答が **§D-3** のファクトと一致（Web Access: No／UGC: No／Tracking: No）
- [ ] 最終IPAのInfo.plist権限文言・icon alpha・暗号申告（`ITSAppUsesNonExemptEncryption: false`）・`PrivacyInfo.xcprivacy`（詳細な確認観点は`RELEASE_PREP.md`§7-4）
- [ ] スクショ（releaseビルド・実UI・価格が写る場合はASC設定と一致）
- [ ] 公開Privacy／Terms／SupportのURLが有効で現行版
- 実機：—／Sandbox：—（App Store Connect作業）

---

## 問題発見時の優先順位

- 🔴 **P0**：クラッシュ／データ消失（migration・全削除・写真）／保存不可／課金してもProにならない／失効でデータが消える
- 🟠 **P1**：上限gateの抜け（11件目・2旅行目・復元）／残り予算の画面間不一致／価格が出ない／復元不可／STT・TTSが止まらない
- 🟡 **P2**：文言の誤読（全体10件に見える等）／権限拒否時の導線不足／軽微なUI崩れ
- 🟢 **P3**：外観・アニメーション・微調整

## 問題メモ

| # | 項目 | 現象 | 優先度 | 対応 |
|---|---|---|---|---|
| | | | | |

## 機械的確認（Build前にClaude Code側で実行済みであること）

Build対象commit `ba2153a` 時点での実測値（2026-09-12・Build前に実行）:

- [x] `npx tsc --noEmit` pass（エラー0）
- [x] `npm test` **331 pass / 0 fail**
- [x] `npm run lint` **20 problems（16 errors / 4 warnings）** ＝ baselineと同一・新規増加なし
- [x] `git diff --check` clean

> 注：`AGENTS.md`のlint baseline記載は「23 problems（16 errors / 7 warnings）」だが、
> 実測は20 problems（16 errors / 4 warnings）。**実測値が正**（warningが3件解消済み）。
