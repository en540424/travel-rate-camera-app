# RevenueCat iOS Public SDK Key
# RevenueCat Dashboard > Project settings > API keys の iOS Public app-specific key を使う。
# 実際の値はここには書かず、ローカルの .env（Git管理外）に自分で入力すること。
# EAS Buildで使う場合は、Build Profileごとに同じ変数名でEAS環境変数（eas env:create 等）を登録する。
EXPO_PUBLIC_REVENUECAT_IOS_KEY=
